# Zygisk - Block .xlDownload

Block MIUI rom generate .xlDownload in internal storage root.

# Build

On Linux:

1. Install `zip`, `Android NDK`

2. Run command
    ```bash
    ANDROID_NDK=/path/to/ndk ./build.sh build
    ```
